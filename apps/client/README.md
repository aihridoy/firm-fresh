# FarmFresh — Client

Next.js (App Router) frontend for FarmFresh. See the [root README](../../README.md) for full setup, tech stack, and project structure.

```bash
pnpm dev --filter=client
```

Runs at http://localhost:3000.
