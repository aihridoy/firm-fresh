PORT=8000

# MongoDB Atlas credentials (cluster0.pfan7vt.mongodb.net)
DB_USER=
DB_PASS=

# Secrets used to sign access/refresh JWTs (use two distinct random values)
JWT_SECRET=
JWT_REFRESH_SECRET=

# Cloudinary (profile picture uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Resend (transactional email - password reset, etc.)
RESEND_API_KEY=
EMAIL_FROM=

# Base URL of the client app, used in emailed links
FRONTEND_URL=http://localhost:3000
